// src/bot/shifts/close.js
function registerShiftClose(bot, ensureUser, logError) {
  // сюда позже добавим shift_close_confirm / shift_close_yes / shift_close_no
}

module.exports = { registerShiftClose };
